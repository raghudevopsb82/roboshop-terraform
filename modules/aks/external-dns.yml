provider: azure
policy: upsert-only

serviceAccount:
  create: true
  name: external-dns

extraVolumes:
  - name: azure-config-file
    secret:
      secretName: azure-config-file

extraVolumeMounts:
  - name: azure-config-file
    mountPath: /etc/kubernetes
    readOnly: true